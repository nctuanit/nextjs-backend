# Project: Next.js Backend (Elysia Nest-like Library)

## Goal
A highly performant, custom backend library built on top of [Bun](https://bun.sh) and [ElysiaJS](https://elysiajs.com).
It implements an architecture heavily inspired by **NestJS**, utilizing:
- ECMAScript Decorators (`reflect-metadata`)
- Dependency Injection Containers (IoC)
- Controllers mapped with Routing Metadata
- Extensible Pipelines (Guards, Interceptors, Pipes, Exception Filters)
- Drop-in compatibility with Next.js App Router API endpoints
- CLI scaffolding tool for rapid project/module creation

## Technology Stack
- **Language**: TypeScript (`bun:test` enabled)
- **Runtime**: Bun (v1.3.10+)
- **Server Core**: ElysiaJS
- **Validation**: `@sinclair/typebox` (native to Elysia) & `class-validator` + `class-transformer`
- **Metadata**: `reflect-metadata`
- **Auth**: `jose` (JWT), `@auth/core` (NextAuth/Auth.js)

## Architectural Guidelines (For AI Agents)

When interacting with this codebase, adopt the following structural paradigms:

1. **Dependency Injection**: 
   - Uses a custom generic `Container` class (`src/di/container.ts`).
   - Supports `useClass`, `useValue`, `useFactory`, `useExisting` provider formats.
   - Classes MUST be decorated with `@Injectable()` to be registered in the IoC Container.
   - Constructor parameters can be overridden with `@Inject(TOKEN)` for custom injection tokens.
   - Modules collect providers, controllers, and imports using `@Module()`.

2. **Controllers & Routing**:
   - Decorate classes with `@Controller('/path')`.
   - Decorate methods with `@Get()`, `@Post()`, `@Put()`, `@Patch()`, `@Delete()`.
   - Extract parameters via `@Param('id')`, `@Query()`, `@Body()`, `@Headers()`, `@Req()`, `@Res()`, `@Session()`.
   - File uploads are explicitly handled via `@File('fieldname')` and `@Files('fieldname')` decorators for `multipart/form-data`.
   - Route mapping and Elysia app generation happens natively in `ElysiaFactory.create(ModuleClass)`.

3. **Pipelines (Execution Order)**:
   - Application execution flow per endpoint: 
     `Middlewares` -> `Guards` -> `Interceptors (pre-hook)` -> `Pipes (Transform/Validate)` -> `Method Execution` -> `Interceptors (post-hook)`.
   - Exception handling is controlled via `@UseFilters()` and `@Catch()`. Filters implement the `ExceptionFilter` interface.
   - Per-route middleware via `@UseMiddleware()` decorator.

4. **Validation**:
   - Prefer validating DTO instances using `@UsePipes(new ValidationPipe())` paired with `class-validator` decorators.
   - Elysia TypeBox (`t.Object`, `t.String`, etc.) is supported gracefully inline. E.g., `@Body(t.Object({ name: t.String() }))`.

5. **Core Capabilities & Built-in Features**:
   - The library has built-in specific enterprise modules and core mechanics:
     - **Global Middlewares (`@Middleware`)**: Cross-cutting Express-like request handlers integrated into the IoC.
     - **Per-Route Middleware (`@UseMiddleware`)**: Apply middleware to specific controllers or methods.
     - **ConfigModule**: Validates `process.env` against a TypeBox schema.
     - **JwtModule**: Generates and verifies JSON Web Tokens using `jose`.
     - **NextAuth (Auth.js)**: Full Auth.js integration with `NextAuthModule`, `NextAuthGuard`, and session handling.
     - **SessionModule**: Cookie-based session tracking with pluggable `SessionStore` (InMemory/Redis). Built-in expired session cleanup.
     - **LoggerService**: Context-aware custom logging utility with ANSI colors.
     - **PasswordService**: Bun native password hashing (bcrypt / argon2).
     - **CacheModule**: Automatic endpoint caching in-memory or Redis via `@CacheKey`, `@CacheTTL` and `CacheStore` interfaces.
     - **WebSocketGateway**: Bidirectional socket handlers with `@WebSocketGateway`, `@SubscribeMessage`, `@WebSocketServer()`, and `WsServer` type.
     - **SSE (Server-Sent Events)**: Stream data using `@Sse('/path')` decorator with `AsyncGenerator` or `ReadableStream` support.
     - **Schedule & Events**: `@Cron()` decorator for scheduled tasks, `EventEmitterService` for pub/sub with `@OnEvent()` listeners.
     - **Health Check Module**: `HealthModule` with `HealthService` and `/health` endpoint for readiness/liveness probes.
     - **DevModeModule (Request Profiler)**: Zero-overhead request execution profiler with built-in dashboard (`/dev/history`, `/dev/stats`).
     - **Rate Limiting**: `@RateLimit()` and `@Throttle()` decorators for per-route or global rate limiting.
     - **Global Exception Filters**: Automatic conversion of Custom HttpExceptions or uncaught errors.
     - **OpenAPI / Swagger**: Supported native by adding `@elysiajs/swagger` to the root Elysia instance.

6. **Next.js Integration Rules**:
   - To integrate into Next.js App Router (`app/api/.../route.ts`), do NOT manual handle Promises. Use the helper method natively:
   - `export const { GET, POST, PUT, PATCH, DELETE } = ElysiaFactory.createNextJsHandlers(AppModule, { globalPrefix: '/api' })`

7. **Client-side Type Safety (Eden Treaty)**:
   - The library supports `@elysiajs/eden` out of the box. 
   - To use Eden, export the type of your Elysia instance: `export type App = typeof app;` and consume it in your frontend using `edenTreaty<App>('http://localhost:3000')`.
   - CLI supports `eden:generate` command for automatic type generation.

8. **CLI Tool**:
   - `npx next-js-backend new <project-name>` — Scaffold a complete project with module structure.
   - `npx next-js-backend g <type> <name>` — Generate module, controller, service, guard, interceptor, or full resource (CRUD).
   - `npx next-js-backend eden:generate <module-path>` — Generate Eden Treaty types.

9. **Testing**:
   - `Test.createTestingModule()` for isolated test environments with provider overrides.
   - Override providers with `overrideProvider(TOKEN).useValue(mock)` for dependency mocking.

10. **Extensibility & Type Augmentation**:
   - AI Agents MUST utilize TypeScript declaration merging (`declare module 'next-js-backend'`) if modifying `SessionData` or `User` objects to retain strict Type safety across the module logic.
   - Code style strongly favors ES Modules (`import`/`export`), Strict TS configs, and pure OOP.
